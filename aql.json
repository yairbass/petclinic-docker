items.find({
  "name":{"$match":"spring-petclinic*.jar"},
  "$and":[
    {"@build.name":{"$eq":"Petclinic-BuildAndUpload"}},
    {"repo":{"$eq":"maven-release-local"}}
  ]
})
.include("*").sort({"$desc":["created"]})

